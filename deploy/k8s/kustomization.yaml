apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - configmap.yaml
  - service.yaml
  - deployment.yaml

images:
  - name: ghcr.io/arvesv/mylinks
    newTag: latest
